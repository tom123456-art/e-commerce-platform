<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="com.example.ecommerce.entity.Order">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="orderNo" column="order_no" />
            <result property="totalAmount" column="total_amount" />
            <result property="status" column="status" />
            <result property="address" column="address" />
            <result property="phone" column="phone" />
            <result property="receiver" column="receiver" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,order_no,total_amount,status,address,
        phone,receiver,create_time,update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        select
            <include refid="Base_Column_List"/>
            from `order`
        where id = #{id}
    </select>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
            from `order`
        where order_no = #{orderNo}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        select
            <include refid="Base_Column_List"/>
            from `order`
    </select>

    <select id="selectByUserIdOrders" resultMap="BaseResultMap">
        select
            <include refid="Base_Column_List"/>
            from `order`
        where user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.example.ecommerce.entity.Order" useGeneratedKeys="true" keyProperty="id">
        insert into `order`(user_id,order_no,total_amount,status,address,phone,receiver,create_time,update_time)
        values(#{userId},#{orderNo},#{totalAmount},#{status},#{address},#{phone},#{receiver},now(),now())
    </insert>

    <!--全字段更新，按照主键id定位-->
    <update id="update" parameterType="com.example.ecommerce.entity.Order">
        update `order`
        set
            user_id = #{userId},
            order_no = #{orderNo},
            total_amount = #{totalAmount},
            status = #{status},
            address = #{address},
            phone = #{phone},
            receiver = #{receiver},
            update_time = now()
        where id = #{id}
    </update>

    <update id="updateStatusByOrderNo">
        update `order`
        set
            status = #{status},
            update_time = now()
        where order_no = #{orderNo}
    </update>

    <delete id="delete">
        delete from `order`
        where id = #{id}
    </delete>
</mapper>
