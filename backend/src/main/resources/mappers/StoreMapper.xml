<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.StoreMapper">

    <resultMap id="BaseResultMap" type="com.example.ecommerce.entity.Store">
        <id column="id" property="id"/>
        <result column="merchant_id" property="merchantId"/>
        <result column="store_name" property="storeName"/>
        <result column="store_description" property="storeDescription"/>
        <result column="store_logo" property="storeLogo"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="contact_email" property="contactEmail"/>
        <result column="address" property="address"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        , merchant_id, store_name, store_description, store_logo, contact_phone, contact_email, address, status, create_time, update_time
    </sql>

    <select id="selectByMerchantId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from store
        where merchant_id = #{merchantId}
    </select>

    <insert id="insert" parameterType="com.example.ecommerce.entity.Store" useGeneratedKeys="true" keyProperty="id">
        insert into store (merchant_id, store_name, store_description, store_logo, contact_phone, contact_email,
                           address, status, create_time, update_time)
        values (#{merchantId}, #{storeName}, #{storeDescription}, #{storeLogo}, #{contactPhone}, #{contactEmail},
                #{address}, #{status}, now(), now())
    </insert>

    <!-- 动态更新：只写入非空字段，避免把未提供的列覆盖为 NULL（与 ProductMapper 同模式） -->
    <update id="update" parameterType="com.example.ecommerce.entity.Store">
        update store
        <set>
            <if test="storeName != null">store_name = #{storeName},</if>
            <if test="storeDescription != null">store_description = #{storeDescription},</if>
            <if test="storeLogo != null">store_logo = #{storeLogo},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="address != null">address = #{address},</if>
            <if test="status != null">status = #{status},</if>
            update_time = now()
        </set>
        where id = #{id}
    </update>

</mapper>