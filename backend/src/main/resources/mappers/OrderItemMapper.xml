<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.example.ecommerce.entity.OrderItem">
            <id property="id" column="id" />
            <result property="orderId" column="order_id" />
            <result property="productId" column="product_id" />
            <result property="productName" column="product_name" />
            <result property="price" column="price" />
            <result property="quantity" column="quantity" />
    </resultMap>

    <sql id="Base_Column_List">
        id,order_id,product_id,product_name,price,quantity
    </sql>
    <insert id="insert" parameterType="com.example.ecommerce.entity.OrderItem"
            useGeneratedKeys="true" keyProperty="id">
        insert into order_item (order_id, product_id, product_name, price, quantity)
        values (#{orderId}, #{productId}, #{productName}, #{price}, #{quantity})
    </insert>
    <insert id="insertBatch" parameterType="java.util.List">
        insert into order_item (order_id, product_id, product_name, price, quantity)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>
    <delete id="deleteByOrderId">
        delete from order_item where order_id = #{orderId}
    </delete>
    <select id="selectAll" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />
        from order_item
    </select>
    <select id="selectByOrderId" resultMap="BaseResultMap">
        select <include refid="Base_Column_List" />
        from order_item
        where order_id = #{orderId}
    </select>
</mapper>
