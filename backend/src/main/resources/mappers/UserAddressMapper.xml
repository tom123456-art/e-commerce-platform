<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.UserAddressMapper">

    <resultMap id="BaseResultMap" type="com.example.ecommerce.entity.UserAddress">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="receiver" property="receiver"/>
        <result column="phone" property="phone"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="detail_address" property="detailAddress"/>
        <result column="is_default" property="isDefault"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        , user_id, receiver, phone, province, city, district, detail_address, is_default, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_address
        where id = #{id}
    </select>

    <!--
      selectByUserId：ORDER BY 多字段排序
      优先级从左到右递减：is_default DESC（默认地址排第一）-> update_time DESC -> id DESC
    -->
    <select id="selectByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_address
        where user_id = #{userId}
        order by is_default desc, update_time desc, id desc
    </select>

    <!-- selectDefaultByUserId：WHERE is_default=1 + LIMIT 1，即使数据异常也只返回一条 -->
    <select id="selectDefaultByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_address
        where user_id = #{userId}
        and is_default = 1
        limit 1
    </select>

    <insert id="insert" parameterType="com.example.ecommerce.entity.UserAddress" useGeneratedKeys="true"
            keyProperty="id">
        insert into user_address (user_id, receiver, phone, province, city, district, detail_address, is_default,
                                  create_time, update_time)
        values (#{userId}, #{receiver}, #{phone}, #{province}, #{city}, #{district}, #{detailAddress}, #{isDefault},
                now(), now())
    </insert>

    <!-- update：WHERE id AND user_id 双重条件，防越权修改他人地址 -->
    <update id="update" parameterType="com.example.ecommerce.entity.UserAddress">
        update user_address
        set receiver       = #{receiver},
            phone          = #{phone},
            province       = #{province},
            city           = #{city},
            district       = #{district},
            detail_address = #{detailAddress},
            is_default     = #{isDefault},
            update_time    = now()
        where id = #{id}
          and user_id = #{userId}
    </update>

    <delete id="delete">
        delete
        from user_address
        where id = #{id}
          and user_id = #{userId}
    </delete>

    <!-- clearDefaultByUserId：把该用户所有地址 is_default 置 0（设新默认前调用） -->
    <update id="clearDefaultByUserId">
        update user_address
        set is_default  = 0,
            update_time = now()
        where user_id = #{userId}
    </update>

</mapper>